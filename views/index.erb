<header>
  <div id="whisper">
  <label for="words">
    <input type="text" name="words" id="words" placeholder="Whisper into the future...">
    <button id="whisper-submit">Say Something Later</button>
  </label>
  </div>
</header>
<p>Count is: <%= @count %></p>
  <% @messages.each do |message| %>
    <p><%= [ message[0], message[1]] %><p>
  <% end %>
<main id = "gallery">

</main>